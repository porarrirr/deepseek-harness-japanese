<!-- Generated by scripts/gen-cordis-catalog.ts — do not edit by hand.
     Run `pnpm run gen-cordis-catalog` to regenerate. -->

# Context

[English](context.md) | [中文](context.zh.md) | 日本語


contextはCordisのcore objectです。すべてのservice、event、lifecycle APIへ`ctx`からアクセスします。event methodは[Events](events.md)、effectとcurrent fiberは[Fiber](fiber.md)、plugin loadingは[Registry](registry.md)に記載しています。

Cordis pluginのrootおよびchild dependency containerです。

contextはproxyです。通常のproperty readはservice resolverを通り、`extend()`、`isolate()`、`intercept()`はparentを変更せずにscope付きchild contextを作成します。

[Source](../../vendor/cordis/src/context.ts#L42)

### ctx.extend(meta?)

```ts cordis-catalog
/**
 * Create a child context with extra metadata on top of the current scope.
 *
 * The child prototypally inherits every property of this context; own
 * properties of `meta` shadow the inherited ones. The parent is not mutated.
 *
 * @param meta — own properties (including symbol keys) to define on the child.
 * @returns a child context inheriting from this one.
 */
extend(meta = {}): this
```

current scopeの上に追加metadataを持つchild contextを作成します。

childはこのcontextのすべてのpropertyをprototype経由で継承し、`meta`のown propertyが継承したpropertyをshadowします。parentは変更されません。

- `meta` — childに定義するown property（symbol keyを含む）。

**Returns** このcontextを継承するchild context。

[Source](../../vendor/cordis/src/context.ts#L99)

### ctx.isolate(name, label?)

```ts cordis-catalog
/**
 * Create a child context with an independent service scope for `name`.
 *
 * Below the returned context, reads and writes of the service `name`
 * resolve against the new label instead of the parent's, so a different
 * implementation can be provided without affecting the parent scope.
 * Passing the same `label` to two `isolate()` calls joins their scopes.
 *
 * @param name — the service name to isolate.
 * @param label — scope label to join; defaults to a fresh unique symbol.
 * @returns a child context whose `name` service resolves in the new scope.
 */
isolate(name: string, label?: symbol)
```

`name`に独立したservice scopeを持つchild contextを作成します。

返されたcontext以下では、service `name`のreadとwriteはparentではなく新しいlabelに対してresolveされます。そのためparent scopeに影響を与えず別implementationを提供できます。2つの`isolate()` callに同じ`label`を渡すとscopeが結合します。

- `name` — isolateするservice name。
- `label` — joinするscope label。defaultは新しいunique symbolです。

**Returns** `name` serviceが新しいscopeでresolveされるchild context。

[Source](../../vendor/cordis/src/context.ts#L121)

### ctx.intercept(name, config)

```ts cordis-catalog
/**
 * Add service-specific intercept config for plugins started below this
 * context.
 *
 * Plugins loaded under the returned context see `config` merged into the
 * service's resolved config (ancestor entries first; see
 * `Service[symbols.resolveConfig]`). The parent context is not affected.
 *
 * @param name — the service name whose config to intercept.
 * @param config — the intercept config to merge for that service.
 * @returns a child context carrying the additional intercept entry.
 */
intercept<K extends InjectKey>(name: K, config: Context[K] extends { [symbols.config]: infer T } ? T : never): this
intercept(name: string, config: any): this
```

このcontext以下でstartするpluginにservice-specific intercept configを追加します。

返されたcontext以下でloadされたpluginは、serviceのresolved configにmergeされた`config`を参照します（ancestor entryが先です。`Service[symbols.resolveConfig]`を参照）。parent contextには影響しません。

- `name` — configをinterceptするservice name。
- `config` — そのserviceにmergeするintercept config。

**Returns** 追加のintercept entryを持つchild context。

[Source](../../vendor/cordis/src/context.ts#L139)

### ctx.root

```ts cordis-catalog
/** The root context of the application (every child context shares it). @experimental */
root: this
```

applicationのroot context（すべてのchild contextが共有します）。@experimental

[Source](../../vendor/cordis/src/context.ts#L22)

### ctx.baseUrl

```ts cordis-catalog
/** Base URL used to resolve relative plugin/module specifiers, if the runtime sets one. */
baseUrl?: string
```

runtimeが設定している場合にrelative plugin／module specifierのresolveに使うbase URL。

[Source](../../vendor/cordis/src/context.ts#L24)

### ctx.events

```ts cordis-catalog
/** The event bus. Its methods are also mixed onto `ctx` (`ctx.on`, `ctx.emit`, ...). */
events: EventsService
```

event bus。そのmethodは`ctx`（`ctx.on`、`ctx.emit`など）にもmixinされます。

[Source](../../vendor/cordis/src/context.ts#L26)

### ctx.logger

```ts cordis-catalog
/** The logging service. Call `ctx.logger(name)` for a named logger. */
logger: LoggerService
```

logging service。named loggerには`ctx.logger(name)`を呼び出します。

[Source](../../vendor/cordis/src/context.ts#L28)

### ctx.reflect

```ts cordis-catalog
/** The reflection layer backing the context proxy (`ctx.get`, `ctx.provide`, ...). */
reflect: ReflectService
```

context proxy（`ctx.get`、`ctx.provide`など）を支えるreflection layer。

[Source](../../vendor/cordis/src/context.ts#L30)

### ctx.registry

```ts cordis-catalog
/** The plugin registry. Its methods are mixed onto `ctx` (`ctx.plugin`, `ctx.inject`). */
registry: RegistryService
```

plugin registry。そのmethodは`ctx`（`ctx.plugin`、`ctx.inject`）にもmixinされます。

[Source](../../vendor/cordis/src/context.ts#L32)

## Static member

### Context.effect

```ts cordis-catalog
/** Symbol key under which a disposer exposes its {@link EffectMeta} diagnostics tree. */
static readonly effect: unique symbol
```

disposerがEffectMeta diagnostics treeを公開するsymbol key。

[Source](../../vendor/cordis/src/context.ts#L44)

### Context.filter

```ts cordis-catalog
/** Symbol key for a context's listener filter, consulted on every event dispatch. */
static readonly filter: unique symbol
```

contextのlistener filter用symbol key。すべてのevent dispatchで参照されます。

[Source](../../vendor/cordis/src/context.ts#L46)

### Context.isolate

```ts cordis-catalog
/** Symbol key of the isolation map (see the `Context[symbols.isolate]` property). */
static readonly isolate: unique symbol
```

isolation mapのsymbol key（`Context[symbols.isolate]` propertyを参照）。

[Source](../../vendor/cordis/src/context.ts#L48)

### Context.intercept

```ts cordis-catalog
/** Symbol key of the intercept map (see the `Context[symbols.intercept]` property). */
static readonly intercept: unique symbol
```

intercept mapのsymbol key（`Context[symbols.intercept]` propertyを参照）。

[Source](../../vendor/cordis/src/context.ts#L50)

### Context.is(value)

```ts cordis-catalog
/**
 * Returns true for Cordis context proxies and context prototypes.
 *
 * Works across realms and across multiple copies of cordis, because the
 * brand is keyed by a global symbol rather than by `instanceof`.
 *
 * @param value — the value to test.
 * @returns `true` if `value` is a Cordis context, narrowing its type.
 */
static is(value: any): value is Context
```

Cordis context proxyおよびcontext prototypeに対してtrueを返します。

global symbolをkeyにしたbrandを使い`instanceof`に依存しないため、realm間および複数のcordis copy間で動作します。

- `value` — 検査するvalue。

**Returns** `value`がCordis contextの場合は`true`。型もnarrowされます。

[Source](../../vendor/cordis/src/context.ts#L61)

## Service storeとmixin

### ctx.get(name, strict?)

```ts cordis-catalog
/**
 * Read a service from the store without the inject requirement.
 *
 * @param name — the service name.
 * @param strict — when `true` (default), only return implementations
 * whose providing fiber is currently active.
 * @returns the service value, or `undefined` when not (yet) provided.
 */
get<K extends string & keyof this>(name: K, strict?: boolean): undefined | this[K]
get(name: string, strict?: boolean): any
```

inject requirementなしでstoreからserviceを読み取ります。

- `name` — service name。
- `strict` — `true`（default）の場合、providing fiberが現在activeなimplementationだけを返します。

**Returns** service value。まだprovideされていなければ`undefined`です。

[Source](../../vendor/cordis/src/reflect.ts#L17)

### ctx.set(name, value)

```ts cordis-catalog
/**
 * Overwrite a provided service's value.
 *
 * Only the fiber that provided the service may set it; setting an
 * unprovided name throws.
 *
 * @param name — the service name.
 * @param value — the new service value.
 */
set<K extends string & keyof this>(name: K, value: undefined | this[K]): void
set(name: string, value: any): void
```

provide済みserviceのvalueを上書きします。

serviceをprovideしたfiberだけが設定できます。未provideのnameを設定するとthrowします。

- `name` — service name。
- `value` — 新しいservice value。

[Source](../../vendor/cordis/src/reflect.ts#L29)

### ctx.provide(name, value)

```ts cordis-catalog
/**
 * Register a service implementation owned by the current fiber.
 *
 * The service becomes visible to dependents in the same isolation scope
 * once the fiber is active; it is unregistered (waking dependents) when
 * the returned disposer runs or the fiber unloads. Throws if the name is
 * already provided in this scope or declared as an accessor.
 *
 * @param name — the service name.
 * @param value — the service value.
 * @returns a disposer that unregisters the service.
 */
provide<K extends string & keyof this>(name: K, value: undefined | this[K]): () => void
provide(name: string, value?: any): () => void
```

current fiberが所有するservice implementationを登録します。

fiberがactiveになると同じisolation scopeのdependentからserviceが見えるようになります。返されたdisposerの実行時またはfiber unload時にunregisterされ（dependentをwakeします）。このscopeでnameがすでにprovide済み、またはaccessorとして宣言済みならthrowします。

- `name` — service name。
- `value` — service value。

**Returns** serviceをunregisterするdisposer。

[Source](../../vendor/cordis/src/reflect.ts#L44)

### ctx.accessor(name, options)

```ts cordis-catalog
/**
 * Define a computed context property backed by get/set hooks.
 *
 * The accessor is removed when the current fiber unloads. Throws if the
 * name is already declared.
 *
 * @param name — the context property name.
 * @param options — the `get` hook and optional `set` hook.
 */
accessor(name: string, options: Omit<Property.Accessor, 'type'>): void
```

get／set hookを使うcomputed context propertyを定義します。

current fiberのunload時にaccessorを削除します。nameがすでに宣言済みならthrowします。

- `name` — context property name。
- `options` — `get` hookと任意の`set` hook。

[Source](../../vendor/cordis/src/reflect.ts#L56)

### ctx.mixin(name, mixins)

```ts cordis-catalog
/**
 * Expose selected members of a service directly on `ctx`.
 *
 * Each mixed-in key becomes an accessor that forwards to the service
 * (binding methods to it), so e.g. `ctx.on` forwards to `ctx.events.on`.
 * Mixins are removed when the current fiber unloads.
 *
 * @param name — the context property holding the source service.
 * @param mixins — keys to forward, or a source-key → ctx-key map.
 */
mixin<K extends string & keyof this>(name: K, mixins: (keyof this & keyof this[K])[] | Dict<string>): void
mixin<T extends {}>(source: T, mixins: (keyof this & keyof T)[] | Dict<string>): void
```

serviceの選択したmemberを`ctx`に直接公開します。

各mixin keyはserviceへforwardするaccessorになります（methodはserviceにbindされます）。たとえば`ctx.on`は`ctx.events.on`へforwardします。current fiberのunload時にmixinを削除します。

- `name` — source serviceを保持するcontext property。
- `mixins` — forwardするkey、またはsource-key → ctx-key map。

[Source](../../vendor/cordis/src/reflect.ts#L67)
